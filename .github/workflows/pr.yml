# This is a basic workflow to help you get started with Actions

name: Build PR

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  pull_request:
      
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Build:
    runs-on: ubuntu-20.04

    env:
      COMMIT_REF: $(git rev-parse --short "$GITHUB_SHA")

    steps:
      - uses: actions/checkout@v2
# https://stackoverflow.com/questions/60300169/how-to-get-branch-name-on-github-action#:~:text=GITHUB_BASE_REF%20is%20the%20short%20branch%20name%20for%20the,PR%2C%20it%20is%20the%20PR%20branch%20name%20%28refs%2Fpull%2F123%2Fmerge%29.
      - name: Get the branch name
        id: get_branch
        run: echo ::set-output name=BRANCH::${GITHUB_BASE_REF}

      - name: Get the version
        id: get_version
        run: echo ::set-output name=VERSION::${GITHUB_REF/refs\/tags\//}

      - name: Run Bicep build
        uses: aliencube/bicep-build-actions@v0.1
        with:
          files: infrasetup/main.bicep

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
            
      - name: Create Resource Group
        run: |
          az group create -g "featherservicetest-rg" -l "West US"
          
      - name: Deploy Azure Resource Manager (ARM) Template
        uses: azure/arm-deploy@v1
        id: deploy
        with:
          scope: resourcegroup
          subscriptionId: ${{ secrets.SUBSCRIPTION_ID }}
          resourceGroupName: featherservicetest-rg
          template: ./infrasetup/main.json
          deploymentMode: Incremental
            
      - name: Build image with ACR
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az acr build --registry ${{ steps.deploy.outputs.registryNameOutput }} --image fst-${{steps.get_branch.outputs.BRANCH}}:latest ./src/BasicService/
